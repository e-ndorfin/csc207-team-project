package com.sketchandguess.interface_adapters.menu;

/**
 * State class for the Main Menu view.
 * Currently minimal as the menu primarily handles navigation.
 */
public class MenuState {
    
    // Menu doesn't need state currently, but structure exists for consistency
    // and potential future features (e.g., showing user stats, recent games, etc.)
    
    public MenuState() {
        // Default constructor
    }
}
